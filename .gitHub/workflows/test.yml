name: Pipeline test

on:
  push:
    branches:
      - dev

jobs: 
  validar-push-dev:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validar existência do index
        run: test -f index.html
